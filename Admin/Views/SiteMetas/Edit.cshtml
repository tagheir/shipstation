@model Generics.Db.SiteMetaDto
<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-body">
                <form  method="post">
                    <input type="hidden" class="form-control" value="@Model.KEY" name="@nameof(Generics.Db.SiteMetaDto.KEY)" />
                    <input type="text" class="form-control" value="@Model.VALUE" name="@nameof(Generics.Db.SiteMetaDto.VALUE)"  />
                    <button type="submit" class="btn btn-primary">
                        Update
                    </button>
                </form>
            </div>
        </div>
    </div>
</div>